var words = [
    "beep",
    "flute",
    "empire",
    "serpent",
    "trench"
];

var word = words[Math.floor(Math.random() * words.length)];

var blanks = [];
for(i=0; i<words.length; i++) {
    blanks[i] = "_";
}

var remaining = word.length;

while (remaining > 0) {
    
}